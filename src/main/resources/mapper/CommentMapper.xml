<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="inu.voucherview.mapper.CommentMapper">

    <resultMap id="CommentMap" type="inu.voucherview.domain.Comment">
        <id column="comment_id" property="commentId"/>
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 댓글 생성 -->
    <insert id="insert" parameterType="inu.voucherview.domain.Comment" useGeneratedKeys="true" keyProperty="commentId">
        INSERT INTO comment (post_id, user_id, content)
        VALUES (#{postId}, #{userId}, #{content})
    </insert>

    <!-- 댓글 조회 -->
    <select id="findById" resultMap="CommentMap">
        SELECT comment_id, post_id, user_id, content, created_at, updated_at
        FROM comment
        WHERE comment_id = #{commentId}
    </select>

    <!-- 게시물의 댓글 목록 조회 -->
    <select id="findByPostId" resultMap="CommentMap">
        SELECT comment_id, post_id, user_id, content, created_at, updated_at
        FROM comment
        WHERE post_id = #{postId}
        ORDER BY created_at ASC
    </select>

    <!-- 게시물의 댓글 개수 조회 -->
    <select id="countByPostId" resultType="int">
        SELECT COUNT(*)
        FROM comment
        WHERE post_id = #{postId}
    </select>

    <!-- 댓글 수정 -->
    <update id="update">
        UPDATE comment
        SET content = #{content},
            updated_at = CURRENT_TIMESTAMP
        WHERE comment_id = #{commentId}
    </update>

    <!-- 댓글 삭제 -->
    <delete id="delete">
        DELETE FROM comment WHERE comment_id = #{commentId}
    </delete>

</mapper>
